{
  "name": "bitsy",
  "version": "8.4.0",
  "description": "make tiny games, worlds, and stories",
  "author": "Adam Le Doux",
  "scripts": {
    "build": "",
    "dev": "",
    "deploy": "",
    "docker-build": "docker build -t je-bitsy .",
    "docker-run": "docker run -it -d -p 8080:80 je-bitsy",
    "docker": "npm run docker-build && npm run docker-run"
  },
  "devDependencies": {
    "ssh2": "^1.11.0"
  }
}
